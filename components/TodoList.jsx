
export const TodoList = ({title,as})=>{
    
    return(
      <div>
      <ul>
        <p>{title}</p>

         {/* {TodoList.map((todo)=>(<TodoItem todo={todo} key={todo.id} />))} */}
      </ul>
      
      </div>
    )
  }
  